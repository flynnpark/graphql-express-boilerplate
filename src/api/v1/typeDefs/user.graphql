extend type Query {
  user: User
}

extend type Mutation {
  emailSignUp(
    email: String!
    password: String!
    name: String!
    avatar: String
    shortBio: String
  ): EmailSignUpResponse!
}

type User {
  id: Int!
  email: String!
  password: String!
  name: String!
  avatar: String
  shortBio: String
  createdAt: String!
}

type EmailSignUpResponse {
  ok: Boolean!
  error: String
  token: String
}
